type: txt
syntax:expression: $VAR(@) in "off", "internal", "kernel"; "Invalid value."
allowed: echo "off internel kernel"
help: How are indirect packets forwraded.
val_help: off; Incoming packets not for the local node are dropped
val_help: internal; Incoming packets are forwarded by tinc internally
val_help: kernel; Incoming packets are always sent to the tun/tap device.
