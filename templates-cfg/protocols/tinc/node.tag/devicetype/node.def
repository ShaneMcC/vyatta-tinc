type: txt
syntax:expression: $VAR(@) in "dummy", "raw_socket", "multicast", "tun", "tap"; "Invalid value."
allowed: echo "dummy raw_socket multicast tun tap"
help: The type of the virtual network device.
val_help: dummy; Use a dummy interface.
val_help: raw_socket; Open a raw socket, and bind it to a pre-existing Interface 
val_help: multicast; Open a multicast UDP socket and bind it to an address and port
val_help: tun; Set type to tun. 
val_help: tap; Set type to tap.
